#pragma once

#include <SFML/Graphics.hpp>
#include <functional>
#include "usefulDefines.hpp"
#include <vector>
#include <limits>

// Implements Notification interface (text on window, which expires after given time)
class Notification : public sf::Drawable {
public:
    Notification();

private:


};